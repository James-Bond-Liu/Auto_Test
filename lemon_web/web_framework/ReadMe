Common--关于PageObjects中所有的公共操作，但是不涉及业务知识
PageLocators--页面中的定位元素，以元组的格式呈现
PageObjects--实现页面中的功能
TestCases--测试用例
TestDatas--测试数据

PageOgjects调用PageLocators + Common
TestCases调用TestDatas + PageObjects

我们在写页面功能代码的时候，不能直接实例化webdriver，因为一条用例是由多个页面的多个功能组合起来的，是一个串行的用例。
如果每个页面都实例化webdriver，那么在用例调用页面功能的时候，会打开多个浏览器多个页面，用例不可能串行起来



















投资用例设计
拥有独立的测试账号

正常用例
前提条件：
1用户已经登录，
2有能够投资的标，如果没有标，可以先通过接口方式加标。
3用户有可用的余额可以投资。（初始余额先给一个亿或者通过接口方式查询当前用户还有多少可用余额，如果不够我们就充值，如果够不用充值）

步骤：
1在首页选标--不根据标名，根据抢投标按钮选标，默认第一个标
2标页面--获取投资前的用户余额，
3标页面--输入投资金额，点击投资按钮，
4标页面--点击投资成功的弹出框，查看并激活，进入个人页面，

断言：
钱--投资后的金额是不是少了投资金额
个人页面--获取投资后的金额，
投资前的金额-投资后的金额=投资金额，
投资记录是否正确？
利息是否正确？

异常用例：非常好创造，非常好写的，